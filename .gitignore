# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/
.env
.venv
venv/
env/

# Databases — live on Railway volume
data/
*.db

# Generated profile output (cards + JSON) — rebuilt from the local DB by
# `main.py profiles` / generate_all_cards; regenerable data, not code.
profiles/output/
profiles/__pycache__/

# Audit / diagnostic scripts
*_audit.py
*_diagnostic.py
*_test.py

# Experimental / research scripts (kept local; the shipped model lives in model/)
eye_test_experiments/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Claude
.claude/

# Large rebuildable train cache (regenerated by `main.py train`; >100MB, never commit)
model/artifacts/feature_cache.parquet
model/artifacts/feature_cache_meta.pkl

# Experimental / abandoned-architecture artifacts (never commit — production uses
# ensemble_all.pkl + the norm_* / *_lookup / baselines files only)
*.rds
model/artifacts/nn_shape*.pt
model/artifacts/nn_*_scaler.pkl
model/artifacts/nn_tj_*.pkl
model/artifacts/cluster_*.pkl
model/artifacts/global_clusters.pkl
model/artifacts/pitch_clusters.pkl
model/artifacts/loc_plus.pkl
model/artifacts/location_*.pkl
model/artifacts/model_xrv_*.json
model/artifacts/model_cs_*.json
model/artifacts/model_swing_*.json
model/artifacts/model_whiff_*.json
model/artifacts/model_contact_*.json
model/artifacts/residual_models.pkl
model/artifacts/pitcher_level_norms.pkl
model/artifacts/*.bak
model/artifacts/*_backup.pkl

# Local scratch eval scripts (session tools, not part of the deliverable)
scratchpad_*.py

# Editor/backup scratch files (never commit)
*.bak
*.darkbak
